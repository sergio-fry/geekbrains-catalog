<%= simple_form_for @category do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :created_at, as: :date %>
  <%= f.button :submit, class: 'btn btn-primary mt-2' %>
<% end %>
